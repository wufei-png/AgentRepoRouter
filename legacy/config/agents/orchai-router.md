# Legacy Router Prompt Example

这个文件仅作为历史参考保留，不参与当前运行时。

当前生效的 Router 入口是：

- 源文件：`skills/router/SKILL.zh.md` 和 `skills/router/SKILL.en.md`
- 部署文件：`~/.openclaw/skills/router/SKILL.md`
- 运行时配置：`~/.openclaw/skills/router/references/repo_mappings.json`

如果需要修改当前 Router 行为，请编辑 `skills/router/` 下的源文件并重新运行 `bash scripts/install.sh`。
